<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    tools:context=".UI.Activity.AddressManageActivity">

    <include
        android:id="@+id/include"
        layout="@layout/address_manage_titelbar" />

    <com.scwang.smart.refresh.layout.SmartRefreshLayout
        android:id="@+id/address"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <com.scwang.smart.refresh.header.ClassicsHeader
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <ListView
            android:id="@+id/address_manage_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="8dp" />

    </com.scwang.smart.refresh.layout.SmartRefreshLayout>

    <Button
        android:id="@+id/address_addNewAddress"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:text="@string/address_add"
        android:background="@drawable/add_address_btn"
        android:textSize="15dp"
        android:textStyle="bold" />
</LinearLayout>
